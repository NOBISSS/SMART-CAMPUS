{
  "name": "smartcampus",
  "version": "1.0.0",
  "description": "smart campus project",
  "main": "server/src/server.js",
  "type": "module",
  "scripts": {
    "start": "concurrently \"node server/src/server.js\" \"npm run client\"",
    "dev": "nodemon -r dotenv/config --experimental-json-modules server/src/server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "nodejs",
    "mongoose",
    "express",
    "backend"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^9.1.2",
    "nodemon": "^3.1.9",
    "prettier": "^3.4.2"
  },
  "dependencies": {
    "dotenv": "^16.4.7"
  }
}
